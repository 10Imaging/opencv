version: '1.0'
steps:
  BuildingDockerImage:
    title: Building Docker Image
    type: build
    image_name: 10imaging/opencv
    working_directory: ./
    dockerfile:
      content: |-
        FROM ubuntu:latest
        RUN ./build.sh clean
    tag: '${{CF_BRANCH_TAG_NORMALIZED}}'